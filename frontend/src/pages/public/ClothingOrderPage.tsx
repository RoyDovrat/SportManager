import { t } from '../../i18n/t'

/** Placeholder — form lands in Stage 2. */
export function ClothingOrderPage() {
  return (
    <section className="admin-page">
      <h1>{t('publicClothing.title')}</h1>
      <p>{t('publicClothing.placeholder')}</p>
    </section>
  )
}
