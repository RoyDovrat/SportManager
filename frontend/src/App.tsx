import { apiBaseUrl } from './config'

function App() {
  return (
    <main>
      <h1>SportManager</h1>
      <p>Frontend setup in progress.</p>
      <p>
        API base URL: <code>{apiBaseUrl}</code>
      </p>
    </main>
  )
}

export default App
