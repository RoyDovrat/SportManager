# Used by `npm run build`. Overrides local `.env` so localhost is not baked into the bundle.
# Empty = same origin (Nginx serves the UI and proxies /api). No trailing slash if you set a full URL.
VITE_API_BASE_URL=
